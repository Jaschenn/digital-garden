---
{"dg-publish":true,"dg-path":"2. 计算机综合/数据结构/5. 树相关/5. 树相关的代码.md","permalink":"/2//5/5/"}
---

```c
// 节点定义
typedef struct TreeNode{
	int data;
	struct TreeNode * left;
	struct TreeNode * right;
} TreeNode;

// 递归遍历,深度优先
void pre_order(struct TreeNode *tree){
	if(tree == NULL){
		return;
	}
	print(tree.data);
	pre_order(tree->lefr);
	pre_order(tree->right);

}

//层次遍历，即 BFS
void level_order(struct TreeNode *tree){
	if(root==NULL){
		return;
	}
	
	struct *Quene quene = createQuene();
	enquene(quene,root);
	
	while(quene.front != NULL){
		struct TreeNode * node = dequene(quene);
		print(node.data);
		
		if(node->left!=NULL){
			enquene(quene,node->left);
		}
		if(node->right != NULL){
			enquene(quene, node->right)
		}
	
	}
}


```